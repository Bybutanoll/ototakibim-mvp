[build]
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NEXT_PUBLIC_API_URL = "https://ototakibim-mvp.onrender.com"

[functions]
  included_files = []

[dev]
  command = "npm run dev"
  port = 3000
  publish = ".next"